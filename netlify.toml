[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # These are intentionally public in a Vite client bundle.
  SECRETS_SCAN_OMIT_KEYS = "VITE_SUPABASE_URL,VITE_SUPABASE_ANON_KEY,SUPABASE_PROJECT_ID"
